# 升级记录:全库分镜按新契约返工完成(2026-09-02 深夜)

## 背景

用户拍板「需要按新契约返工」——存量 6 本 524 章 16977 镜按分镜师契约 H-N 全量返工。

## 返工方法(tools/rework_camera_contract.py)

**LLM 出补丁 + 程序手术 + 契约校验兜底**(不动台词不动剧情,只重分配运镜):
1. 每章凝练镜头清单 → deepseek 选出需转运动镜的镜头并为每镜写贴合主体的运镜句(输出小 JSON 补丁,恰 need 项,宁少勿多);
2. 程序应用:camera 列替换 + dd 静态镜头句→运镜句替换 + 气闸补写/运镜句置首(transform 同全库调优版安全规则:台词句不搬/连接词清理);
3. 契约后检:H 仍超→程序兜底转换(静态长镜→近景特写→全景远景优先);静态长镜强制转;K 兜底(运镜列动了但 dd 无运镜句→按运镜列生成通用句插入);
4. `<d>` 数/[Shot N] 断言零变化才落盘。

批量后追加三轮程序化收尾:连续固定段破段(每段转中位镜,收敛到 0)、双气闸去重(保护台词,63 镜中文段落遗留保留)、裸 `Shot N` 补括号(7 文件)。

## 成果(修前 → 修后)

| 指标 | 修前 | 修后 | 契约 |
|---|---|---|---|
| 固定镜占比 | **73.7%** | **34.5%** | ≤50% ✅ |
| H 未达标章 | ~490 章 | **0 章** | 0 ✅ |
| I 连续固定段 | 557 | **0** | 0 ✅ |
| N 静态长镜(≥8s) | 1451 | **0** | 0 ✅ |
| M 人群个体化 | — | **0** | 0 ✅ |
| 运动镜缺气闸 | 496 | 0(批量内全覆盖) | 0 ✅ |

转换量:LLM 精选 ~4400 镜 + 程序兜底/破段 ~430 镜。

## 已知残留(有意保留)

- **K 运镜句置首 368**:多为含 `<d>`/`<Subject` 的运镜句(搬动破坏叙事序,设计性跳过);渲染端 CAMERA 纪律(已内联 dd 段前)兜底;
- **L 屏内容 70**:渲染端 SCREEN CONTENT guard 全库自动兜底(68 镜触发率已验);
- **双气闸标记 63**:中文段落遗留镜(英气闸+英连接句引出中文正文块,老格式),非真重复,不动避免误删正文;
- 少量双运镜句冗余(通用句+原句同向,H3 双读无害)。

## 验证

- 全程 `<d>` 台词数断言零变化;[Shot N] 标记全库零缺失;
- 抽样质量:气闸→贴合主体运镜句(「pushes in slowly toward the girl's bright eyes」)→站位,形态合规;
- enc_guard(novel 1384+ 文件)/deliver_check PASS;
- 各书脚本指纹已变,下次渲染自动重解析全量重渲(配合渲染端运动镜断链+纪律瘦身,运镜/站位将真正执行)。
