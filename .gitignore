# 构建产物(用 build.bat 重新编译)
NiliX.exe
NiliX-Capsule.exe
NiliX-Main.exe
capsule/build/
capsule/capsule_test.exe
mainwin/build/
mainwin/mainwin_test.exe
*.exe.old

# 运行时数据与密钥(绝不入库)
logs/
.secret.key
server/settings.json
settings.json

# 过期资源(仅保留 amd64)
rsrc_windows_386.syso

# 本地开发临时
*.tmp
*.bak
.DS_Store
__pycache__/

# 窗口尺寸运行时状态(用户拖动落盘,勿入库)
mainwin.json
# 热替换编译备份
NiliX.exe~
NiliX.exe.bak-*
# --mainwin 子进程 WebView2 用户数据目录(截图/调试启动产生)
NiliX.exe.WebView2/

# 运行时数据目录(漫剧项目/小说库,自包含部署后位于 exe 目录旁,勿入库)
manju/

# 会话误产 JSON 草稿(文件名 "...tmp"+时间戳)与运行时目录
...tmp*
server/
ComfyUI/
petwin.json

# go test 误落在包目录的 analysis 运行产物(正常应在 t.TempDir)
internal/api/analysis/
